import styled from 'styled-components';
import tw from 'tailwind-styled-components';

const LogoStyle = styled.h1`
  font-size: 22px;
`;
export const Logo = tw(LogoStyle)`text-white1`;

export const Pink = tw.span`text-pink1`;
